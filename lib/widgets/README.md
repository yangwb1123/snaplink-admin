# lib/widgets — 组件库（20+ 组件）

状态：`status_chip.dart`（双编码徽章，对比度门禁守护）
数据：`stat_card.dart` / `count_up.dart` / `sparkline.dart` /
      `progress_ring.dart` / `admin_data_table.dart`（表格：排序/斑马纹/
      hover/复制/批量）
结构：`admin_list_header.dart` / `admin_breadcrumb.dart` / `empty_state.dart` /
      `section_selector.dart` / `paginated_list.dart` / `skeleton_list.dart` /
      `async_view.dart` / `hover_card.dart`
交互：`pressable_scale.dart` / `page_transition.dart` / `staggered_fade_in.dart` /
      `batch_selection.dart` / `confirm_dialog.dart` / `command_palette.dart`

规则：新页面从组件库组装（不重复造轮子）；组件遵守 8pt 间距 token。
