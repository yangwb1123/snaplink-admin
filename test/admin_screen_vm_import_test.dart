import 'package:flutter_test/flutter_test.dart';
import 'package:sso_admin/screens/admin/dashboard_screen.dart';

void main() {
  test('admin dashboard dependency graph is importable on the VM', () {
    expect(DashboardScreen, isNotNull);
  });
}
