"""Engineering checks for sso-admin Flutter project.

Each module exposes a `run()` function that returns 0 for pass, 1 for fail.
The checks are driven by engineering.yaml in the project root.
See `python cli.py help` for available commands.
"""
