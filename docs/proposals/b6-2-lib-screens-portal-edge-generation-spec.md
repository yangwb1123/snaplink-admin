# B6-2 Requirements Specification — Anchor edge-generation verification on the real `/auth/login` emitters (SSOAdminClient / OidcLoginScreen with client_id=sso-admin-console) and prove the portal paste-login path emits nothing

Module: `lib/screens/portal` (analysis bucket `docs/auto/analyses/lib-screens-portal-44bdd36d.json`, direction 2) · Direction: B6-2 (edge generation, `implementation-gate.md:57` row 2) · Value: 10 · Risk reduction: 9 · Effort: 2 · Confidence: 9 (selected-direction re-score; spec refreshed and every citation re-verified 2026-08-08 at HEAD `e1073ce` + working tree)
Status: **verification-only requirements**. The direction's theses hold at HEAD: (1) `SSOAdminClient.login` and the unified hosted login are the only credential-bearing `POST /auth/login` emitters, both defaulting to `client_id=sso-admin-console`; (2) the portal paste/resume path is a `GET /me` probe only and contains zero `/auth/login` references. The deliverables below pin these facts as **repo guards + request-bound tests + the existing joint drill**, so the contract's "login → sink `auth.login.success`（client_id=sso-admin-console）；无重复" acceptance (`implementation-gate.md:57`) cannot be derailed into treating the portal token-paste surface as an edge generator. Change set: **test-only** — zero `lib/` and zero production-code changes are required or permitted (the emitters and the non-emitter are existing code; this direction only proves and pins them).

> Sibling coordination: `docs/proposals/b6-2-lib-api-client-id-alignment-spec.md` (REQ-0 Branch B, REQ-1 `SSOAdminClient.firstPartyClientId`) and the landed census anchor (`docs/proposals/b6-2-lib-screens-oidc-login-handle-success-anchor-spec.md`) jointly own the `client_id` literal census (`test/oidc_login_handle_success_census_test.dart`, §6.4 gate c) and the hosted-login exactly-once harness (`test/oidc_login_screen_client_id_test.dart`). This spec preserves those guards in **every landing ordering** (§3 REQ-2, AC-4).

## 1. Problem statement (verified)

- `docs/campaigns/implementation-gate.md:57` (console row 2) demands: 边缘生成验证：login → `auth.login.success`（client_id=sso-admin-console）| sink 出现 sso-admin-console login 事件；无重复 | B4-5. The **edge generator** is therefore any code path that produces a login credential submission carrying `client_id=sso-admin-console`; the **verification** is that each login yields exactly one such submission and the sink shows exactly one row — no duplicates.
- The module under analysis contains the riskiest misread: `lib/screens/portal`'s "login" is a **bearer-token paste/resume validated only by `GET /me`** (`PortalApi.login`, `lib/api/portal_api.dart:233-257`; both entry points `lib/screens/portal/portal_screen.dart:119` resume / `:211` paste). It issues **no** `POST /auth/login` (repo-wide grep: zero `/auth/login` occurrences in `lib/screens/portal/`). It is therefore **not an edge generator** and cannot double-emit — it must never be treated as one by an implementer of the B6-2 acceptance.
- The real emitters are exactly two:
  1. `SSOAdminClient.login` (`lib/api/sso_client.dart:83-97`) — direct password-grant `POST /auth/login` with body `'client_id': clientId` at `:92`, default `'sso-admin-console'` at `:86`, `'credential': {username, password}` at `:96`, `auth: false`.
  2. The unified hosted login — `OidcLoginScreen(defaultClientId: 'sso-admin-console')` at `lib/app_router.dart:35`, whose `_effectiveClientId` (`lib/screens/oidc_login/oidc_login_screen.dart:161`) flows into `OidcLoginApi._login` → `POST ../auth/login` with `'client_id': clientId` (`lib/api/oidc_login_api.dart:51-58`).
- The existing census already pins that `auth.login.success` is **never fabricated client-side** (`test/oidc_login_handle_success_census_test.dart:97-104` scans `lib/screens/oidc_login/` and asserts the emission string is absent; repo-wide `grep -rn "auth\.login\.success" lib/` → exit 1 at HEAD). The event originates server-side; this repo can only verify the request side (exactly-once, correct client_id) and the sink side (drill).
- **Gaps this direction closes** (verified at HEAD):
  - `test/sso_client_test.dart:10-27` asserts the `/auth/login` body shape **inside** the MockClient handler but does **not** count requests — "exactly one credential-bearing POST per `login()`" is unpinned for the `SSOAdminClient` leg (the hosted leg already pins it: `test/oidc_login_screen_client_id_test.dart` `loginPosts == 1` at `:135/:149/:155`).
  - No portal test asserts the request bound "zero `/auth/login` POSTs; `GET /me` only" for the paste and resume paths (existing `test/portal_entry_test.dart:167-195, :236+` assert Bearer header / 403 preservation, not the zero-emission bound).
  - The sink-side joint leg exists as a **tracked, wired drill** (`tests/integration/audit_login_drill.py`, wired at `tests/integration/run_all.py:169` and `tests/integration/full_stack_verify.py:113`) — steps 4/5 (exactly one `auth.login.success` row; no duplicates) and step 6 (console-shaped read `GET /api/v1/audit/events?tenant_id=<t>&limit=100`, the exact `AuditQuery` wire). This spec pins it as the executable form of acceptance (c), preserving its `[proposed]` fallback (no false PASS without a deployed stack).

## 2. Evidence verification table (every direction citation re-checked at HEAD + working tree)

| # | Citation from direction | Verified repository reality | Status |
|---|---|---|---|
| E1 | `lib/api/sso_client.dart:85-100` — login `POST /auth/login`, default `clientId 'sso-admin-console'` at `:86`, `'client_id'` in body at `:92` | `login` at **:83-97** `[CORRECTION: 85-100 → 83-97; method body closes :97]`; default `:86` `String clientId = 'sso-admin-console',`; `_post('/auth/login', {` at :90 with `auth: false`; `'client_id': clientId,` at **:92**; `'credential': {'username':…, 'password':…}` at :96. `login()` is the only `/auth/login` request in the file (all other `_post` targets are `/api/v1/admin/*`) | ✅ exact |
| E2 | `lib/app_router.dart:35` — `OidcLoginScreen(defaultClientId: 'sso-admin-console')` | `:34-36` — `ProductEntry.login => OidcLoginScreen(` at :34, `defaultClientId: 'sso-admin-console',` at **:35**, `api: oidcLoginApi, routeUri: location` at :36-37 | ✅ exact |
| E3 | `lib/api/portal_api.dart:233-284` — login = `GET /me` only, no `/auth/login`; rolls back token on non-200 | `login` at **:233-257** `[CORRECTION: 233-284 → 233-257]`; installs token then `final r = await get('/me');` at :245; non-200 → `throw PortalApiError` :247; `catch` rolls back `_token/_sessionId/_clientId` :250-253; `:233-284` additionally covers `signOut` :259-263, `logout` :268 (`post('/logout')`), `fetchMe` :270-276 — no `/auth/login` anywhere in the file (`grep -n "auth/login" lib/api/portal_api.dart` → exit 1); post-login traffic is the SSE read `GET /me/notifications/stream` :125 | ✅ exact (method span corrected) |
| E4 | `lib/screens/portal/portal_screen.dart:119, :211` — resume + paste both go through `PortalApi.login` / `GET /me` | `_tryResumeSession` (:108-~175) calls `_api.login(token, sessionId:…, clientId:…)` at **:119**; `_login()` (paste, :199-~227) calls `_api.login(t)` at **:211**; `_api = widget.api ?? PortalApi()` at :90; both methods route exclusively through `PortalApi.login` → `GET /me` | ✅ exact |
| E5 | `test/oidc_login_handle_success_census_test.dart:97-104` — 'auth.login.success' absent from `lib/screens/oidc_login` | Test `'no auth.login.success emission string in the module (REQ-4 #5)'` at **:97-104**; scans `moduleDir = 'lib/screens/oidc_login'` (:16) for the literal; identical at HEAD and in the working tree — the file is **byte-identical** (`git diff HEAD -- test/oidc_login_handle_success_census_test.dart` → empty): the 30-test count gate and the topology group are **HEAD** content (after :109), leaving :97-104 and the pinnedSites set untouched. Repo-wide: `grep -rn "auth\.login\.success" lib/` → exit 1; in `test/` only the census file itself contains the string | ✅ exact (module-scoped census; repo-wide invariant holds) |
| E6 | `docs/campaigns/implementation-gate.md:57` — B6-2 acceptance: sink shows sso-admin-console login event; no duplicates | Row at **:57** exact: `\| 2 \| console \| lib/ 原生事件 \| 边缘生成验证：login → auth.login.success（client_id=sso-admin-console）\| sink 出现 sso-admin-console login 事件；无重复 \| B4-5 \|`. Dependency B4-5 = sink-side row generation (out of this repo) | ✅ exact |

Supplemental verified facts that shape the requirements:

| # | Fact | Verification |
|---|---|---|
| S1 | `test/sso_client_test.dart` is the direction's named harness pattern | Test at :10-27: MockClient asserts `method == 'POST'`, `url.path == '/auth/login'`, and the **whole decoded body** including `'client_id': 'sso-admin-console'` at :18 (a census-pinned literal site, census `pinnedSites`). Assertions run in-handler → a second POST would also pass; no request counter exists — the exactly-once pin is the gap |
| S2 | Hosted-login leg already pins exactly-once + client_id | `test/oidc_login_screen_client_id_test.dart`: harness `_LoginHarness` at **:26-103** counts `loginPosts` filtered by the D9 `body['credential'] is Map` check (probe POSTs excluded), captures `lastClientId`; HEAD assertions: `loginPosts == 0` at :117 (probe-excluded case), `loginPosts == 1` at :135 and :149, `lastClientId == 'sso-admin-console'` at :136 and :158, `loginPosts == 2` at :155 (3 testWidgets). Constantization comments (:20-23, :134, :156) document the sibling `SSOAdminClient.firstPartyClientId` M2 co-change — in the working tree the file is already constantized (import + `defaultClientId` / `lastClientId` reference the constant) |
| S3 | Hosted login's wire: `OidcLoginApi._login` | `lib/api/oidc_login_api.dart:51-58` — `_postOutcome('../auth/login', {'client_id': clientId, …})` at :51-52 (password flow) and :58 (code-sender); `_resolve` at :39 resolves against the product origin |
| S4 | `_effectiveClientId` chain | `lib/screens/oidc_login/oidc_login_screen.dart:161` — `_params.clientId.isNotEmpty ? _params.clientId : widget.defaultClientId ?? ''`; with a bare `/login/` URL the fallback is `app_router.dart:35`'s `'sso-admin-console'` |
| S5 | Portal module has zero `/auth/login` | `grep -rn "auth/login" lib/screens/portal/` → exit 1 (zero hits; 33 files incl. the `portal_api.dart` shim) |
| S6 | Post-login portal traffic is BFF GETs only | After successful resume/paste, `_initializeNotifications()` (`portal_screen.dart:134`, :222) → `notificationEvents()` → `GET /me/notifications/stream` (`portal_api.dart:125`) — a `/me`-rooted GET, never `/auth/login` |
| S7 | Sink joint drill exists, tracked, wired | `tests/integration/audit_login_drill.py` (tracked in git; `git ls-files` confirms). `AGREED_CLIENT_ID = 'sso-admin-console'` (:31, Branch B). Step 3: real `POST {PROXY}/auth/login` with `CONFIG.login_payload()` (the `sso_client.dart:92` wire). Step 4 (:169-190): `GET /api/v1/audit/events?event_types=auth.login.success&tenant_id=<t>` → **exactly one row with `client_id == AGREED_CLIENT_ID`** (:182-185). Step 5 (:192-216): second identical login → exactly two rows, no repeated event id/trace_id, count stable after re-settle. Step 6 (:219-265): console-shaped `GET /api/v1/audit/events?tenant_id=<t>&limit=100` (string-serialized `limit` — the exact `AuditQuery` wire) → caller-attributed `audit.event.read`. Sink legs are `[proposed]`-marked, never false PASS, when no stack/credentials (`IntegrationConfigurationError` → SKIP, exit 0; unverifiable rows → `proposed = True`) |
| S8 | Console-shaped read wire | `lib/api/audit_read_client.dart:15-17` (trio path literals) / `list()` :41-51 (`AuditQuery(...).toQueryParameters()` → `_api.get(eventsPath, query:)`); default wire `{'limit':'100'}` pinned at `test/audit_query_test.dart` AC-1.1 and `test/audit_read_client_test.dart` |
| S9 | `client_id` literal census rule (both revisions) | `test/oidc_login_handle_success_census_test.dart:109-146` — if `static const String firstPartyClientId` exists in `lib/api/sso_client.dart` (:114 `constantDecl`): **zero** `'sso-admin-console'` literals in `test/`; else literal census == exactly `pinnedSites` (`sso_client_test.dart:18`; `oidc_account_flow_test.dart:35,75,115,160`; `oidc_login_screen_client_id_test.dart:76,136,158`). The 30-test count gate (census 10 / client_id 3 / sso 17 = 30) is **HEAD** content (`git show HEAD` confirms; the census file is byte-identical in the working tree), keeping the acceptance command's 30 tests honest against silent deletions. The scanner matches the contiguous quoted literal, so the census file itself self-hosts via a split literal (:113) — a technique this spec **forbids for new production-meaningful sites** (REQ-2) |
| S10 | Existing portal paste/resume test patterns | `test/portal_entry_test.dart` (`@TestOn('browser')`): paste-with-MockClient at :167-195 (asserts `GET /me` 200 + `Authorization: Bearer bearer-token`, POST `/me/invitations/accept` once), stored-session 403-preservation at :236+ (`MockClient` asserts every `request.url.path == '/me'`); `Session.store` / `Session.clear` usage in `tearDown` — ready-made skeletons for the request-bound test |

**Executed at HEAD:** `grep -rn "auth/login" lib/screens/portal/` → exit 1; `grep -rn "auth\.login\.success" lib/` → exit 1; `grep -n "auth/login" lib/api/portal_api.dart` → exit 1; `git ls-files tests/integration/audit_login_drill.py` → tracked.

**Re-verified 2026-08-08 (spec refresh, HEAD `e1073ce` + working tree):** every E1-E6 / S1-S10 citation re-checked — `sso_client.dart:83-97` (login; default `:86`, `'client_id'` `:92`, `credential` `:96`, `auth: false`), `portal_api.dart:233-257` (login = `GET /me` at :245, rollback :250-253; `signOut` :259, `logout` :268, `fetchMe` :270; `notifications/stream` :125), `portal_screen.dart:119/:211` (`_api.login(` call sites), `app_router.dart:35` (`defaultClientId`), `oidc_login_api.dart:51-58`, `oidc_login_screen.dart:159-161` (`_effectiveClientId`), `sso_client_test.dart:10-27` (in-handler body assert, `client_id` site :18, **no request counter**), client_id test HEAD assertions :117/:135/:136/:149/:155/:158, census :109-146 + `pinnedSites` (all **eight** literal sites confirmed at HEAD — 1+4+3: `sso_client_test:18`, `oidc_account_flow_test:35,75,115,160`, `oidc_login_screen_client_id_test:76,136,158`) + 30-test gate, `audit_login_drill.py:31` + steps 3-6 (:141/:169/:192/:219) + wiring (`run_all.py:169`, `full_stack_verify.py:113`). Also re-confirmed: HEAD `lib/api/sso_client.dart` has **no** `firstPartyClientId` (pinned-mode baseline) while the working tree has it at :82 (constant-mode) — both census orderings covered by REQ-2's two-state rule; `docs/proposals/b6-2-lib-screens-portal-edge-generation-design.md` still **absent** (design-stage deliverable).

## 3. Requirements

### REQ-1 — The portal module is not an edge generator: zero `/auth/login` and zero emission strings (repo guard)
`lib/screens/portal/` (33 files including the `portal_api.dart` re-export shim) keeps **zero** `/auth/login` references (path literals, requests, comments) and **zero** `auth.login.success` emission strings. Both invariants hold at HEAD (S5; §2 executed greps) and must be **codified** so a future implementer cannot "fix" B6-2 by adding a login-emission surface to the portal client or by treating `PortalApi.login` as an emitter.

- New guard test `test/edge_generation_portal_negative_test.dart` (or equivalent; name at design stage): scans every `.dart` file under `lib/screens/portal/` for the substrings `auth/login` and `auth.login.success` and asserts zero hits, mirroring the census technique at `test/oidc_login_handle_success_census_test.dart:97-104` (which itself remains untouched).
- No production-code change is involved: `PortalApi.login` stays a `GET /me` probe (`portal_api.dart:233-257`) and the two portal entry points stay at `portal_screen.dart:119/:211`.

**Testable:** `grep -rn "auth/login" lib/screens/portal/` → exit 1; `grep -rn "auth\.login\.success" lib/` → exit 1; `flutter test test/edge_generation_portal_negative_test.dart` green.

### REQ-2 — Console emitters: exactly one credential-bearing `POST /auth/login` per login, body `client_id == 'sso-admin-console'` (unit harness, census-compatible in every landing ordering)
The console's credential submission paths must each issue **exactly one** credential-bearing `POST /auth/login` per login, whose body map contains `client_id == 'sso-admin-console'` and a `credential` map (the direction's acceptance (a), `SSOAdminClient` harness per `test/sso_client_test.dart` pattern).

- **New harness** `test/edge_generation_console_login_test.dart` (plain `test()` + `MockClient`, per `test/sso_client_test.dart:10-27`): a request-recording MockClient counts every request; `client.login('admin', 'password')` must produce **exactly one** `POST` to `/auth/login` whose decoded body contains `'client_id' == 'sso-admin-console'` and `'credential'` as a map carrying the submitted username/password; a second identical `login()` call must produce exactly one more (cumulative 2) — the unit-level "no duplicate emission per login" (mirrors the hosted-leg retry pin at `test/oidc_login_screen_client_id_test.dart:155`). No other requests are permitted during `login()` (the client performs no probe).
- **Census compatibility (mandatory, both orderings — §6.4 gate c):** the assertion site for the client_id value is written in the two-state form used by the sibling files (`oidc_login_screen_client_id_test.dart:20-23, :134, :156`): if `SSOAdminClient.firstPartyClientId` exists in `lib/api/sso_client.dart` (sibling REQ-1, Branch B), the harness compares against the constant (compile-time reference, zero literals — census constant-mode green); otherwise the literal may appear at exactly one site in the new file **and** that site is added to the census `pinnedSites` co-change in the same commit (census pinned-mode green). String-construction evasion (split literals like the census's own internal `"'sso-admin-" "console'"`) is forbidden for this site.
- The hosted-login leg of the same requirement is **already pinned**: `test/oidc_login_screen_client_id_test.dart` (3 testWidgets, `loginPosts == 1` per submit, `lastClientId` — S2); it stays untouched and green.

**Testable:** `flutter test test/edge_generation_console_login_test.dart test/sso_client_test.dart test/oidc_login_screen_client_id_test.dart test/oidc_login_handle_success_census_test.dart` all green; the counting assertion fails if `login()` ever issues 0, 2+, or any non-`/auth/login` credential request.

### REQ-3 — Portal paste/resume paths issue zero `/auth/login` POSTs; `GET /me` is the only credential validation (request-bound widget test)
The portal token-paste path (`portal_screen.dart:_login` :199-~227 → `_api.login(t)` :211) and the resume path (`_tryResumeSession` :108-~175 → `_api.login(token,…)` :119) must be proven request-bound: **zero** `POST /auth/login` (indeed zero POSTs of any kind), with `GET /me` as the sole credential-validation request. This is what makes it structurally impossible for the paste surface to create spurious or duplicate sink events (the direction's acceptance (b)).

- New testWidgets in `test/portal_entry_test.dart` (already `@TestOn('browser')` with `Session` teardown and MockClient patterns, S10):
  1. **Paste path:** `PortalApi(httpClient: recordingMockClient)`; pump `PortalScreen(api: api, redirectMissingSessionToLogin: false)`; `enterText` a bearer token; tap Continue; after settle, assert: zero recorded requests with `method == 'POST'`; exactly one `GET /me` carrying `Authorization: Bearer <pasted-token>`; every other recorded request (if any — the SSE read `GET /me/notifications/stream` at `portal_api.dart:125` may fire) is a `GET` whose path starts with `/me`.
  2. **Resume path:** `Session.store(token, …)`; pump `PortalScreen(api: api, redirectMissingSessionToLogin: false)`; assert the same bounds (exactly one `GET /me` validation; zero POSTs; no `/auth/login`), following the stored-session skeleton at :236+.
- The recording MockClient `fail()`s on any `POST` and on any path not under `/me`.

**Testable:** `flutter test --platform chrome test/portal_entry_test.dart` green (browser-only file — the un-flagged VM command runs zero tests; GitHub `make test-browser`'s fixed file list excludes it, so the automated chrome gate is the gitea full-suite chrome run plus this command); each test fails on any `/auth/login` request, any POST, or any missing `GET /me` validation.

### REQ-4 — Sink joint leg (B4-5): exactly one `auth.login.success` row for sso-admin-console, no duplicates — via the tracked drill, `[proposed]` semantics preserved
The sink-side half of the T-12 joint acceptance (c) is a **contract acceptance** (B4-5): after one console login, `GET /api/v1/audit/events` (query via `AuditQuery`-shaped parameters) returns exactly one `auth.login.success` row for client `sso-admin-console`, and a second identical login does not duplicate the first row. Its executable form is the existing tracked drill `tests/integration/audit_login_drill.py` (S7), which must stay runnable and wired:

- Step 3: one real `POST {PROXY}/auth/login` with `client_id == 'sso-admin-console'` (`AGREED_CLIENT_ID` :31) — the same wire `SSOAdminClient.login` sends (`sso_client.dart:90-96`).
- Step 4: filtered sink read `GET /api/v1/audit/events?event_types=auth.login.success&tenant_id=<t>` returns **exactly one** row with `client_id == 'sso-admin-console'`.
- Step 5: a second identical login yields exactly two rows total, no repeated event id/trace_id, count stable after re-settle (the "无重复" check).
- Step 6 (joint): the console-shaped read `GET /api/v1/audit/events?tenant_id=<t>&limit=100` — the exact `AuditQuery` serialized wire (`audit_query.dart`, pinned by `test/audit_query_test.dart` AC-1.1) — succeeds and, once B1-5 lands, yields a caller-attributed `audit.event.read` row; until then `[proposed]`, never a false PASS.
- `[proposed]` preservation (direction confidence note): with no deployed stack/credentials the drill SKIPs (exit 0, `IntegrationConfigurationError`); unverifiable sink legs are marked `[proposed]` in its output and the deviation is recorded in the `[RESOLVED]` notes (`docs/proposals/audit-contract-batch-snaplink-console.md:10/:13`). No false PASS, no false FAIL.
- Wiring stays intact: listed in `tests/integration/run_all.py:169` and `tests/integration/full_stack_verify.py:113`.

**Testable:** `python3 tests/integration/audit_login_drill.py` → exit 0 (PASS or SKIP) against any environment; against a deployed stack (G2/B4-5 gate), PASS requires steps 4-5's exactly-one/no-duplicate assertions to hold; `grep -n "audit_login_drill" tests/integration/run_all.py tests/integration/full_stack_verify.py` hits.

### REQ-5 — Census guards stay green; the emission string never appears in `lib/` (fixtures only, `test/` only)
The direction's acceptance (d) is preserved as an invariant plus the standing guards:

- `grep -rn "auth\.login\.success" lib/` → exit 1 at all times (verified at HEAD; the event is generated server-side, never fabricated in this repo). The string may appear in `test/` only (fixtures/assertions) — today its sole `test/` occurrence is the census file itself.
- `test/oidc_login_handle_success_census_test.dart` stays green: the emission-string census (:97-104), the `client_id` literal census (:109-146) in both orderings (REQ-2's two-state rule), and the 30-test count gate (HEAD content — the census file is byte-identical in the working tree). This direction adds **no** tests to the three count-gated files (`census` 10 / `client_id` 3 / `sso` 17) and performs **no** `lib/` edits, so the gate is untouched.
- The new test files from REQ-2/REQ-3 are additive (`test/edge_generation_console_login_test.dart`, and portal tests appended to `test/portal_entry_test.dart` — which is not count-gated).

**Testable:** `flutter test test/oidc_login_handle_success_census_test.dart test/oidc_login_screen_client_id_test.dart test/sso_client_test.dart` green; `grep -rn "auth\.login\.success" lib/` → exit 1.

## 4. Acceptance checks (supplied checks preserved 1:1, made testable)

**AC-1 — Direction acceptance (a): exactly one credential-bearing `POST /auth/login` with `client_id == 'sso-admin-console'` per console login.**
1. New `SSOAdminClient` harness (REQ-2): recording MockClient; `login('admin', 'password')` → exactly one `POST /auth/login`; decoded body map contains `client_id == 'sso-admin-console'` (two-state census form) and `credential` = `{username, password}`; second login → cumulative 2, never 1-then-0 or 2-in-one. `flutter test test/edge_generation_console_login_test.dart` green.
2. Hosted-leg leg already pinned: `test/oidc_login_screen_client_id_test.dart` `loginPosts == 1` / `lastClientId == 'sso-admin-console'` (:135-136, :149, :155, :158) — stays green untouched.
3. Status at HEAD: the SSOAdminClient counting harness does **not** exist (S1 gap) — it is the implement-stage addition; `test/sso_client_test.dart` itself is untouched (17-test count gate).

**AC-2 — Direction acceptance (b): portal paste/resume request log contains `GET /me` only — zero `/auth/login` POSTs.**
1. New request-bound testWidgets in `test/portal_entry_test.dart` (REQ-3): paste path and stored-session resume path each assert zero POSTs (in particular zero `/auth/login`), exactly one credential-validating `GET /me`, and any other traffic confined to `GET /me/*` BFF reads. `flutter test --platform chrome test/portal_entry_test.dart` green (browser-only file — un-flagged VM runs zero tests).
2. Guard form: `grep -rn "auth/login" lib/screens/portal/` → exit 1 (REQ-1) — the token-paste surface cannot create spurious or duplicate sink events.
3. Status at HEAD: no such request-bound assertion exists (S10 gap) — implement-stage addition.

**AC-3 — Direction acceptance (c): sink shows exactly one `auth.login.success` row for sso-admin-console; a second identical login does not duplicate the first row.**
1. Executable form: `python3 tests/integration/audit_login_drill.py` — step 4 exactly-one-row (`client_id == 'sso-admin-console'`), step 5 no-duplicates (two rows after second login, distinct ids, stable after re-settle), step 6 console-shaped `limit=100` read (the `AuditQuery` wire, `audit_query_test.dart` AC-1.1 green). Exit 0 = PASS or SKIP; sink legs `[proposed]` when unverifiable (REQ-4), matching the direction's confidence note — never a false PASS.
2. Wiring pins: `tests/integration/run_all.py:169`, `tests/integration/full_stack_verify.py:113`.
3. Status at HEAD: drill exists, tracked, wired, with Branch B value `'sso-admin-console'` (`audit_login_drill.py:31`) — nothing to add for this leg; the sink-side semantics remain a contract acceptance (B4-5) until observed on a deployed stack.

**AC-4 — Direction acceptance (d): census guards stay green — the event string never appears in `lib/` (fixtures only, `test/` only).**
1. `grep -rn "auth\.login\.success" lib/` → exit 1 (verified at HEAD).
2. `flutter test test/oidc_login_handle_success_census_test.dart` green (emission-string census :97-104; literal census :109-146 under REQ-2's two-state rule; 30-test count gate at HEAD).
3. Zero `lib/` changes in this direction's change set (REQ-5) — the emitters and the non-emitter are existing code; `git diff --stat lib/` clean for this direction.

## 5. Out of scope (explicitly not changed by this direction)

- Any sink/IdP-side code, client registry, or row-generation change (B4-5 is a dependency, evidenced only via the drill).
- The Branch A/B `client_id` value decision and the `SSOAdminClient.firstPartyClientId` constant — owned by the sibling `b6-2-lib-api-client-id-alignment-spec.md` (REQ-0/REQ-1); this spec only consumes the constant when it exists (REQ-2).
- The audit read path, `AuditQuery` wire, tenant/trace derivation — owned by B6-1/B6-1a (`audit_read_client.dart`, guard scans; `[RESOLVED]` records at `audit-contract-batch-snaplink-console.md:10/:13`).
- The drill's device-leg URL-shape step (step 2) and any edit to `tests/integration/audit_login_drill.py` — it already satisfies acceptance (c); its header lens ("device redirect-leg facts") is a documentation observation, not a defect to fix here.
- Any change to the three count-gated test files (`oidc_login_handle_success_census_test.dart`, `oidc_login_screen_client_id_test.dart`, `sso_client_test.dart`) except the pre-sibling `pinnedSites` co-change required by REQ-2.
