# B6-1 Requirements Specification — Extend the B6 boundary guard to `lib/screens/developer`: zero case-insensitive `audit` occurrences (scan 6 parity with the portal module)

> Source direction: **"Extend the B6 boundary guard to lib/screens/developer: pin zero audit-ring references and zero audit literals (parity with portal scan 6 and oidc_login AC-2)"** (value 8 / risk 8 / effort 3 / confidence 9), from `docs/auto/analyses/lib-screens-developer-3899da21.json`, direction 1.
>
> Verification basis: **HEAD `3b22ed4`** ("docs: B6-advance campaign round — designs/specs from analysis+review stages"), re-verified 2026-08-08. The direction is **implemented and committed at `4333f59`** ("verify(b6-1): developer module ring-isolation floor + negative boundary"): `scanDeveloperAuditBoundary` (scan id `developer-audit-boundary`) is wired into `scanLibDirectory`, the F2 pin and the three supplied mutation probes landed, and the guard suite was **re-executed** at HEAD — `flutter test test/audit_contract_guard_test.dart test/audit_contract_guard_mutation_test.dart test/developer_audit_visibility_guard_test.dart test/oidc_login_audit_visibility_guard_test.dart` → **79/79 green** (per-file measured: guard 37 + mutation 40 + developer guard 1 + oidc guard 1; matches the G7 re-pin at `docs/campaigns/implementation-gate.md:79`).
> This spec pins the requirement set and the testable acceptance checks; the implementation record is `docs/proposals/b6-1-lib-screens-developer-negative-boundary-design.md` (verified re-pin: `...-design.verified.md`, committed together with this spec). Citations below are against HEAD `3b22ed4` (each cited span re-verified via `git show HEAD:`); deltas from the direction text are marked `[CORRECTION]`.

## 1. Problem statement (verified)

The B6 verification pins two of the three unauthenticated product entries but not the developer one:

- **Scan 6 (portal negative boundary)** — `test/audit_contract_guard_scans.dart:465-475`: zero case-insensitive `audit` occurrences (identifiers, comments, literals, doc comments) anywhere in `lib/screens/portal/`, enforced through `scanLibDirectory` (`:107-129`, per-file call at `:123`) and the `portal-audit-boundary` scan id. The scan early-returns for every non-portal file (`_scanNegativeBoundary` `:433-456`, gated on `modulePrefix`).
- **AC-2 (oidc_login ring guard)** — `test/oidc_login_audit_visibility_guard_test.dart:25-47`: zero `AuditLogService` / `audit_log_service` / `sso_audit_log` references in `lib/screens/oidc_login`.
- **Developer (was the unpinned gap)** — `lib/app_router.dart:32` at HEAD `3b22ed4` (`ProductEntry.developer => const DeveloperScreen(),`; the sibling B6-2 delta is now **committed**, so :32 is identical in the working tree) is a path-routed, **unauthenticated** entry (`app_router.dart:14` documents `/developer/` behind the same reverse proxy). Its transport, `DeveloperApi` (`lib/screens/developer/developer_api.dart:39`, 188 lines), is a standalone `http.Client` hitting only `/.well-known/openid-configuration` (`loadDiscovery` `:52`), `/register` (`register` `:75-92`, `_registerBody` `:94-110`) and `/register/:client_id` (`loadApp` `:117-131`, `saveApp` `:136-152`, `deleteApp` `:155-166`), authenticated solely by `registration_access_token` — zero audit surface today (grep-verified: `grep -rniE "audit|bff" lib/screens/developer/` → exit 1 across all **13** module `.dart` files; `git diff HEAD -- lib/screens/developer/` → empty), and now **pinned** by this direction's landed scan.

What did **not** fail before this direction landed, if planted into a developer file (each verified against the pre-landing scans):

| Planted regression | Scan 1 (audit-path literals) | Scan 2 (bff) | Scan 5 (second-consumer + trio-owner) | Scan 6 (portal boundary) | Landed AC-2 developer guard |
|---|---|---|---|---|---|
| `import 'package:sso_admin/api/audit_read_client.dart';` + read via client constants (no `query:`) | green (no `api/v1/audit` token) | green | green (scan-5 trigger needs `query:`; no trio literal → trio-owner green) | **not scanned** (portal-only root) | green (needles are the 3 ring names only) |
| bare `'/api/v1/audit'` literal (grouping anchor) | green (bare prefix allowlisted — `scans.dart:85` const, allowlist check in `_checkAuditToken` at `:169-190`) | green | green | **not scanned** | green |
| `AuditLogService` import in a comment or via alias (`as auditSvc`) | green | green | green | **not scanned** | green |
| doc comment mentioning "audit trail" | green | green | green | **not scanned** | green |
| `'// bff'` comment | green | **trips** (lib-wide, `scanBffLiterals` at `scans.dart:199-219`) | green | **not scanned** | green |
| full trio literal `'/api/v1/audit/events'` | green (trio allowlisted anywhere) | green | **trips** (`trio-literal-owner`, lib-wide) | **not scanned** | green |

So the genuinely unpinned developer surface was the **wide case-insensitive `audit` net** — identifiers, imports (`audit_read_client`, `audit_query`), comments, doc strings, non-path strings, and the bare `/api/v1/audit` prefix — exactly the class scan 6 pins for portal, plus the **second-read-path** hazard (`AuditReadClient` identifier indirection with no `query:` argument, the E1/E5 evasion shapes the portal skins already trip on at `test/audit_contract_guard_mutation_test.dart:349-410`). The direction's own examples `'AuditLogService' import` and `'/api/v1/audit/events' literal` both contain the substring `audit` and therefore both trip a scan-6-style check; the `'bff' token` example trips scan 2 (already lib-wide) — the developer probes make module-level coverage explicit. `[CORRECTION: the direction's "nothing fails if a future change adds ... a 'bff' token" is imprecise — scan 2 is lib-wide today; the unpinned surface was the case-insensitive 'audit' net and module-level probe coverage.]`

Why the boundary must be pinned: B6-1's sole-owner rule (the audit trio is owned by `AuditReadClient`, `scanTrioLiteralOwnership` at `scans.dart:365-407`, scan 5) and B6-2's single-login-edge rule both depend on every module boundary being pinned. The developer module was the only one of the three unauthenticated entries (`portal`, `oidc_login`, `developer`) without a module-level audit negative pin. The ring-needle half of the direction's title **was already landed** — `test/developer_audit_visibility_guard_test.dart` (AC-2 needles for `lib/screens/developer`, from `docs/proposals/b6-1-lib-screens-developer-ring-isolation-spec.md` REQ-1) — so this direction's delta is exactly the scan-6 extension plus its probe coverage, now implemented.

## 2. Evidence verification table (every direction citation re-checked)

| # | Citation from direction | Verified repository reality | Status |
|---|---|---|---|
| E1 | `test/audit_contract_guard_scans.dart` — scan 6 portal-only roots; `scanAuditPathLiterals`/`scanBffLiterals`/`scanPortalAuditBoundary` | `scanLibDirectory` at `:107-129` (per-file loop calls `scanAuditPathLiterals` `:119`, `scanBffLiterals` `:120`, `scanRawStringification` `:121`, `scanSecondConsumer` `:122`, `scanPortalAuditBoundary` `:123`, **`scanDeveloperAuditBoundary` `:124`**, `scanRingStorageSeam` `:125`; `scanTrioLiteralOwnership` post-loop `:127`). `scanPortalAuditBoundary` at `:465-475` (doc `:458-464`), `_auditAnyPattern = RegExp(r'audit', caseSensitive: false)` at `:431`, shared `_scanNegativeBoundary` helper at `:433-456`, scan id `portal-audit-boundary` at `:472`. Scan id inventory doc `:51-54`; scan 6/6b/7 doc bullets `:28-41`. | ✅ exact (HEAD `3b22ed4`) |
| E2 | `test/oidc_login_audit_visibility_guard_test.dart` — AC-2 zero ring references, oidc_login-only | `moduleDir = 'lib/screens/oidc_login'` at `:25`; `const banned = ['AuditLog' 'Service', 'audit_log_' 'service', 'sso_audit_' 'log']` at `:26` (literal-split so the guard never self-hits); recursive `dart:io` scan `:28-38`; `expect(offenders, isEmpty)` at `:39-47`. | ✅ exact |
| E3 | `lib/app_router.dart:31` — ungated `DeveloperScreen` entry | **Re-pinned to `:32` at HEAD `3b22ed4`** (identical in the working tree): `git show HEAD:lib/app_router.dart` → `:32 = ProductEntry.developer => const DeveloperScreen(),` — unauthenticated (no `AdminGateScreen` wrapper; contrast `ProductEntry.admin => const AdminGateScreen()` at `:34`); `app_router.dart:14` documents `/developer/` behind the shared reverse proxy. `[CORRECTION: the sibling B6-2 delta (added `import 'api/sso_client.dart';` + `defaultClientId: SSOAdminClient.firstPartyClientId`) is now **committed** (`db6e435`/`a2b04fc`) — the arm sits at `:32` at both HEAD and working tree (`git diff HEAD -- lib/app_router.dart` is empty); the offset is attributable to the B6-2 direction, not this one.]` | ✅ exact (HEAD `:32`) |
| E4 | `lib/screens/developer/developer_api.dart` — register/loadApp/saveApp/deleteApp; zero audit/ring surface, grep-verified | `class DeveloperApi` at `:39` (188 lines); `loadDiscovery` (GET `/.well-known/openid-configuration`) `:52`; `registerMetadata` `:62`; `register` (POST `/register`) `:75-92`; `_registerBody` `:94-110`; `loadApp` (GET `/register/:client_id`) `:117-131`; `saveApp` (PUT) `:136-152`; `deleteApp` (DELETE) `:155-166`; `_handle` `:168-187`. `grep -rniE "audit|bff" lib/screens/developer/` → exit 1 (all **13** module `.dart` files clean); `git diff HEAD -- lib/screens/developer/` → empty. `[CORRECTION: the direction lists only register/loadApp/saveApp/deleteApp; loadDiscovery/registerMetadata also exist — all clean.]` | ✅ exact substance |
| E5 | `test/audit_contract_guard_test.dart` — green-against-current-tree tests at `:35,:140,:171,:426` | Working-tree positions: `:41` AC-3.1 scan-1 green; `:115` AC-3.2 scan-2 green; `:146` AC-3.3 catalog-trio green; `:177` scan-5 green; `:184` trio-owner green; `:322` scan-6 portal green; `:441` **scan-6b developer green**; `:508` scan-7 green; `:606` AC-3.4 raw-stringification green. `[CORRECTION: lines shifted +6..+15 vs the HEAD citation because the F2 pin, the scan-6b group, and the scan-7 group landed in the same file.]` | ✅ (positions refreshed) |
| E6 | `test/audit_contract_guard_test.dart:67,:320` — synthetic-probe trees to mirror | `:73-91` = bare-anchor per-function probe test (the direction's `:67`; current position `:73`); `:60-71` = encodeComponent probes; **F2 wiring pin** `:328-436` (the direction's `:320`; scan-6 group `:320-437`): throwaway temp-dir tree, dirty probes per scan id — portal boundary `screens/portal/_probe.dart` `'audit'` `:355-358`, **developer boundary `screens/developer/_probe.dart` `'AUDIT'` `:359-362`** (uppercase needle at `:361` pins `caseSensitive: false`), scan-1 `:363-366`, scan-2 `'// bff'` `:367`, scan-4 `:368-371`, scan-5 `:372-376`, trio owner `:379-384`; `allPerFileScans` `:385-393` includes **`'developer-audit-boundary'`** `:387`; per-id dirty asserts `:394-404`; clean control writes `:405-425` (developer probe neutralized to `'activity'` `:408-411`); per-id green asserts `:426-435`. | ✅ (line spans refreshed at HEAD `3b22ed4`) |
| E7 | `test/audit_contract_guard_test.dart` stays green at HEAD with zero `lib/` edits | **Executed**: guard suite **79/79 green** at HEAD `3b22ed4` (guard 37 + mutation 40 + developer guard 1 + oidc guard 1 — per-file measured by `flutter test -r compact`). `lib/screens/developer/` contains zero case-insensitive `audit`/`bff` (E4) and **zero diff vs HEAD**; the sibling-direction `lib/` changes (`lib/api/sso_client.dart`, `lib/app_router.dart`, `lib/screens/admin/audit_log_tab.dart`, `lib/services/audit_log_service.dart`) are **committed** (B6-2 at `db6e435`/`a2b04fc`, B6-1b at `80f61ec`) — the working tree has **zero** `lib/` diffs. | ✅ executed (79/79) |
| E8 | T-12 joint: the audit guard suite runs in the joint gate | CI "Unit tests" step runs `make test` (`.github/workflows/ci.yml:49-50`) → `flutter test` (all of `test/`, `Makefile:51-53`) + python unit tests; guard files are auto-discovered. Campaign gate G7 (B6) runs the pin + liveness suites in the **same** CI gate (`docs/campaigns/implementation-gate.md:79`): "pin 与 liveness 套件**同门运行**，`@TestOn('vm')` 静默跳过即计数不匹配 → CI 失败"; the G7 count assertion was **re-pinned to 79** (guard 37 + mutation 40 + developer guard 1 + oidc guard 1 — 2026-08-08 `-r expanded` measured, "已含 B6-1 负边界 8 与后续 b6-1a/b6-1b 同文件追加"). T-12 joint per `docs/campaigns/implementation-gate.md:56` (console row 1: server-fed timeline; devtools 伪造不再构成证据). | ✅ exact |

Supplemental evidence (verified):

| # | Fact | Location |
|---|---|---|
| S1 | The developer module's ring-needle guard: `test/developer_audit_visibility_guard_test.dart` (AC-2, same shape as the oidc_login sibling, module root `lib/screens/developer`), plus `test/developer_ring_isolation_test.dart` and `test/developer_forge_invisibility_test.dart` from `docs/proposals/b6-1-lib-screens-developer-ring-isolation-spec.md` — `[CORRECTION: all three test files first appear in `4333f59` itself (`git log --diff-filter=A` on the three paths → `4333f59`; `4333f59^`/`3b64c58` lack all three) — only the ring-isolation **specs** landed earlier (`26782c5`)]` — this direction's scan-6 extension **complements** them (wide net), does not replace them | `test/developer_audit_visibility_guard_test.dart:23-48` |
| S2 | The **landed** scan-6b implementation: shared `_scanNegativeBoundary` helper (`scans.dart:433-456`) parameterized over (module prefix, scan id, module label, boundary reason) keeps `scanPortalAuditBoundary` (`:465-475`) behavior and detail string **byte-identical** — pinned by the V1 test at `audit_contract_guard_test.dart:489-505` asserting the portal detail string exactly | `test/audit_contract_guard_scans.dart:433-497` |
| S3 | The **landed** per-function probes (direction acceptance probes 1-3) in the scan-6b group `audit_contract_guard_test.dart:439-506`: green live-module `:440-445`; probe 1 `AuditLogService` import `:447-458`; probe 2 `'/api/v1/audit/events'` literal `:460-471`; probe 3 `bff` token via `scanBffLiterals` `:473-487`; V1 portal detail pin `:489-505` | `test/audit_contract_guard_test.dart:439-506` |
| S4 | The **landed** mutation skins: group `'B6-1 scan 6 — developer negative boundary fails closed'` at `test/audit_contract_guard_mutation_test.dart:411-463` — skin A (AuditReadClient import + usage in `developer_api.dart`) `:417-434`, skin B (`'/register'` → `'/api/v1/audit'` repoint) `:436-448`, skin C (appended `/// audit probe comment`) `:450-462`; each asserts `mutated != source` (anchor live) then trips `developer-audit-boundary` through `_scanWith`. `_scanWith` default dispatch set `:38-45` includes `developer-audit-boundary`; dispatch block at `:70-72` | `test/audit_contract_guard_mutation_test.dart` |
| S5 | Scan-6b violation detail names the developer module ("the developer module is the B6-1 negative boundary — DCR reads/writes belong to DeveloperApi's /register surface; audit reads belong to SnaplinkAdminApi via AuditReadClient"), keeping per-scan-id pins meaningful (mirror of the portal wording) | `test/audit_contract_guard_scans.dart:485-497` |
| S6 | The F2 pin's developer probe uses the **uppercase** `'AUDIT'` needle (`guard_test.dart:361`), pinning `caseSensitive: false` — a case-sensitive regression of the shared regex would leave the dirty tree green on the developer id while the portal id still trips | `test/audit_contract_guard_test.dart:360-361` |
| S7 | Direction-scope guard: `git diff HEAD --stat -- lib/` is **empty** (the sibling-direction `lib/` changes are committed — B6-2 `db6e435`/`a2b04fc`, B6-1b `80f61ec`); `lib/screens/developer/` has zero diff; the change set for this direction is test-only (3 guard files), committed at `4333f59` | git status / git diff |

## 3. Requirements

### REQ-1 — Zero case-insensitive `audit` occurrences in `lib/screens/developer/` (scan 6 extension, scan id `developer-audit-boundary`)
`test/audit_contract_guard_scans.dart` must carry a developer negative-boundary scan with the **same semantics as scan 6** (`:465-475`): zero case-insensitive `audit` substrings — identifiers, imports, comments, doc comments, string literals, string keys — anywhere in `lib/screens/developer/**.dart`.

- Exported `scanDeveloperAuditBoundary(String source, String fileLabel)` returning `List<AuditGuardViolation>` with scan id **`developer-audit-boundary`** (`:485-497`), implemented via the shared `_scanNegativeBoundary` helper (`:433-456`) parameterized over (module prefix `screens/developer/`, scan id, module label, boundary reason) so `scanPortalAuditBoundary` (`:465-475`) stays **byte-identical** in behavior and message (its `portal-audit-boundary` id, prefix gate, and detail text must not change — pinned by the V1 test at `guard_test.dart:489-505`).
- Violation detail names the developer module as the B6 negative boundary (S5).
- Scan id inventory doc (`:51-54`) and the scan doc bullets (`:28-41`) list `developer-audit-boundary` and the developer module root.
- Compliance at HEAD was already proven (E4: grep exit 1 over all 13 module files) — REQ-1 requires **zero `lib/` edits** (S7: no diff under `lib/screens/developer/`).
- **Status: implemented and committed at `4333f59`** (`scans.dart:485-497`, `:124`); green-against-tree test at `guard_test.dart:440-445`.

### REQ-2 — `scanLibDirectory` wiring + non-vacuous F2 pin coverage
- `scanLibDirectory` (`:107-129`) invokes `scanDeveloperAuditBoundary` in the per-file loop (`:124`), so the green-against-tree tests and the F2 pin exercise the developer root through the real loop.
- `test/audit_contract_guard_test.dart` scan-6b group (`:439-506`) carries:
  1. a green-against-tree test mirroring the portal one (`:321-327`): `scanLibDirectory(packageLibDir())` filtered by `developer-audit-boundary` → empty (`:440-445`);
  2. F2 pin extension (`:359-362` dirty `screens/developer/_probe.dart` with the uppercase `'AUDIT'` needle at `:361`, `:387` `allPerFileScans` entry, `:408-411` clean control neutralizing to `'activity'`) — a forgotten loop call or prefix/id regression fails the pin on the developer id, exactly as it does for `portal-audit-boundary`.
- **Status: implemented and committed at `4333f59`** (all spans above present at HEAD `3b22ed4`; F2 pin executed green in the 79/79 run).

### REQ-3 — Mutation probes fail closed (direction acceptance probes, mirrored from `audit_contract_guard_test.dart:73,:320`)
The three supplied probes must each trip through the guard suite when planted into a developer file (in-memory/temp-dir only; no repo file touched):

1. **`AuditLogService` import** — an import line containing `AuditLogService` in a `screens/developer/`-labeled source → `developer-audit-boundary` violations non-empty (probe 1, `guard_test.dart:447-458`).
2. **`'/api/v1/audit/events'` literal** — a trio literal in a `screens/developer/`-labeled source → `developer-audit-boundary` non-empty (probe 2, `guard_test.dart:460-471`; the wide net trips regardless of the scan-1 trio allowlist and regardless of `trio-literal-owner`).
3. **`bff` token** — `// bff` in a `screens/developer/`-labeled source → `bff-literals` non-empty (probe 3, `guard_test.dart:473-487`; scan 2 is lib-wide; the probe pins that the developer module is inside the guarded tree).

Placement (mirror of the cited probe trees): (a) per-function probes in `test/audit_contract_guard_test.dart` scan-6b group calling `scanDeveloperAuditBoundary`/`scanBffLiterals` directly with `screens/developer/...` labels (the `:73` probe style), and (b) `test/audit_contract_guard_mutation_test.dart`: `_scanWith` default dispatch set (`:38-45`) includes `developer-audit-boundary` (dispatch block `:70-72`), and the group `'B6-1 scan 6 — developer negative boundary fails closed'` (`:411-463`) mirrors the portal skins against `_libSource('screens/developer/developer_api.dart')` — skin A: AuditReadClient import + usage; skin B: DCR register POST repointed at the audit bare prefix; skin C: appended `/// audit probe comment`. Each skin asserts `mutated != source` (anchor still live) then trips the developer scan id through `_scanWith`.
- **Status: implemented and committed at `4333f59`** (probes 1-3 + skins A/B/C; executed green in the 79/79 run).

### REQ-4 — T-12 joint contract: guard suite green in the joint gate, zero `lib/` edits
- The complete audit guard suite — `test/audit_contract_guard_test.dart`, `test/audit_contract_guard_mutation_test.dart`, `test/developer_audit_visibility_guard_test.dart`, `test/oidc_login_audit_visibility_guard_test.dart` — runs in the joint gate via `make test` (CI "Unit tests" step, E8) and must pass unchanged alongside the landed ring-isolation/forge suites.
- **Zero production-code change**: no edit under `lib/` (the module is already compliant, E4; S7); the direction's change set is the three guard test files.
- The landed developer AC-2 (`test/developer_audit_visibility_guard_test.dart`), AC-1 (`developer_ring_isolation_test.dart`), AC-3 (`developer_forge_invisibility_test.dart`) keep passing **unchanged** — the new scan subsumes but does not replace them.
- **Status: verified** — suite 79/79 green in the current tree; G7 count assertion re-pinned to 79 at `docs/campaigns/implementation-gate.md:79`.

## 4. Acceptance checks (supplied checks preserved 1:1, made testable)

**AC-1 — Extend `scanLibDirectory`/`scanPortalAuditBoundary` roots to `lib/screens/developer` (REQ-1 + REQ-2).**
1. `test/audit_contract_guard_scans.dart` exports `scanDeveloperAuditBoundary` (scan id `developer-audit-boundary`, module prefix `screens/developer/`, same zero-case-insensitive-`audit` semantics and violation shape as `scanPortalAuditBoundary`), and `scanLibDirectory` invokes it per file (`:124`).
2. Green-against-tree test in `test/audit_contract_guard_test.dart` scan-6b group: `scanLibDirectory(packageLibDir()).where((v) => v.scan == 'developer-audit-boundary')` → empty (`:440-445`).
3. F2 wiring pin extended: dirty temp tree includes `screens/developer/_probe.dart` with the uppercase `'AUDIT'` needle (`:360-361`), `allPerFileScans` includes `'developer-audit-boundary'` (`:387`), clean control neutralizes it to `'activity'` (`:408-411`).
   **Executable**: `flutter test test/audit_contract_guard_test.dart` → **37/37 green** (measured).

**AC-2 — `test/audit_contract_guard_test.dart` stays green at HEAD with zero `lib/` edits.**
Executable: (a) `git diff HEAD --stat -- lib/screens/developer/` → **empty** (S7; the sibling B6-2/B6-1b `lib/` changes are committed at `db6e435`/`a2b04fc`/`80f61ec` — zero working-tree `lib/` diffs, and they are out of this direction's change set); (b) `flutter test test/audit_contract_guard_test.dart test/audit_contract_guard_mutation_test.dart test/developer_audit_visibility_guard_test.dart test/oidc_login_audit_visibility_guard_test.dart` → **79/79 green** (measured 2026-08-08; baseline at HEAD `e1073ce` was 56/56 before the scan-6b/scan-7 additions — the change set only adds tests to the guard files, never edits `lib/`).

**AC-3 — Mutation probes trip (mirror of the existing synthetic-probe trees at `audit_contract_guard_test.dart:73,:320`).**
Executable probes (in-memory/temp-dir; no repo file touched; all four executed green in the 79/79 run):
1. A source labeled `screens/developer/developer_api.dart` containing an `AuditLogService` import line → `scanDeveloperAuditBoundary` returns non-empty (`developer-audit-boundary`) — `guard_test.dart:447-458`.
2. The same labeled source containing an `'/api/v1/audit/events'` literal → `developer-audit-boundary` non-empty — `guard_test.dart:460-471`.
3. The same labeled source containing `// bff` → `scanBffLiterals`/`scanLibDirectory` returns non-empty (`bff-literals`) — `guard_test.dart:473-487`.
Plus the drill group in `test/audit_contract_guard_mutation_test.dart:411-463` (skins A/B/C on `_libSource('screens/developer/developer_api.dart')` through the extended `_scanWith` dispatch `:38-45`, `:70-72`) — each asserts `mutated != source` and trips `developer-audit-boundary` (skins A/B) or `bff-literals` (probe 3 path).

**AC-4 — T-12 joint: the audit guard suite runs in the joint gate and must pass unchanged.**
Executable: `make test` green at CI (`.github/workflows/ci.yml:49-50`, `Makefile:51-53`) with the extended guard suite (auto-discovered); the landed developer AC-1/AC-2/AC-3 tests (`test/developer_ring_isolation_test.dart`, `test/developer_audit_visibility_guard_test.dart`, `test/developer_forge_invisibility_test.dart`) pass unchanged; campaign G7 gate count assertion re-pinned to **79** (`docs/campaigns/implementation-gate.md:79`: guard 37 + mutation 40 + developer guard 1 + oidc guard 1, measured `-r expanded`; pin + liveness suites run in the same gate so `@TestOn('vm')` silent skips cannot pass).

## 5. Scope guard, dependencies, and expected change set

**In scope (all under `test/`, all landed and committed at `4333f59`):**
- `test/audit_contract_guard_scans.dart` — shared `_scanNegativeBoundary` helper; `scanDeveloperAuditBoundary` (scan id `developer-audit-boundary`); `scanLibDirectory` loop wiring at `:124`; doc inventory updates (REQ-1/REQ-2). `scanPortalAuditBoundary` behavior byte-identical (V1 pin).
- `test/audit_contract_guard_test.dart` — scan-6b group: developer green-against-tree test; F2 pin extension (dirty `'AUDIT'` probe + `allPerFileScans` entry + clean control); per-function probes 1-3 (AC-3); V1 portal-detail pin.
- `test/audit_contract_guard_mutation_test.dart` — `_scanWith` dispatch default set + block gains `developer-audit-boundary`; developer skins group A/B/C (S4 mirror).

**Out of scope (no expansion):**
- **Zero `lib/` edits** — `lib/screens/developer/**` is already compliant (E4) and has zero diff vs HEAD (S7); no production-code change anywhere in `lib/` for this direction. (The sibling-direction `lib/` changes — `lib/api/sso_client.dart`, `lib/app_router.dart`, `lib/screens/admin/audit_log_tab.dart`, `lib/services/audit_log_service.dart` — are **committed** in the B6-2 (`db6e435`/`a2b04fc`) and B6-1b (`80f61ec`) directions and are not attributable here.)
- The landed developer ring-isolation change set (`test/developer_ring_isolation_test.dart`, `test/developer_audit_visibility_guard_test.dart`, `test/developer_forge_invisibility_test.dart`, `docs/proposals/b6-1-lib-screens-developer-ring-isolation-spec.md`) — referenced as the landed AC-2 half of the direction's title; untouched.
- The B6-1b ring-storage-seam scan 7 (`ring-storage-seam`, `scans.dart:499+`) — sibling direction that landed in the same guard files; out of scope except that it shares the three test files.
- Other unauthenticated entries (`setup`, `device_verify`) — the device module's audit-read-client gap is sibling scope (`docs/proposals/b6-1a-lib-screens-device-audit-read-client-gap-spec.md`); this direction pins only `lib/screens/developer`.
- B6-2 (login-edge attribution, `auth.login.success` census, client_id alignment) — sibling directions (`b6-2-*` proposals).
- The BFF audit read / `trace_id`/`tenant_id` wire and sink-side self-audit (B1-5, cross-repo).

**Dependencies:** none — the extension compiles against the existing scan infrastructure and is green at HEAD `3b22ed4` without any other change set. Remaining work for this direction: commit this spec together with the verified design (`...-design.verified.md`) and confirm the joint gate (`make test`) passes with the re-pinned G7 count (79).
