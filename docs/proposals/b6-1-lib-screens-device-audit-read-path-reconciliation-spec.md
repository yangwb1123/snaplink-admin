# B6-1 Requirements Specification — Reconcile the contract's named read path (`lib/api/portal_api.dart`) with the landed owner (`lib/api/audit_read_client.dart` via SnaplinkAdminApi) and ground the tenant_id/trace_id wire

Module: `lib/screens/device` (analysis bucket `docs/auto/analyses/lib-screens-device-fb030ae1.json`) · Direction: B6-1 ("Reconcile the contract's named read path…") · Value: 8 · Risk reduction: 7 · Effort: 6 · Confidence: 7
Status: implemented and verified (2026-08-20) — acceptance checks 1–3 are landed: the gate names the proven carrier, portal-audit-boundary covers both portal scopes, and G7 is re-pinned at +83.
Completes: the residual of `docs/campaigns/implementation-gate.md:56` row 1 (contract text vs landed carrier), after `b6-1a-lib-screens-device-audit-read-client-gap-spec.md` (REQ-1/REQ-2/REQ-6 landed) and `b6-1-lib-screens-portal-negative-boundary-spec.md` (scan 6 for `lib/screens/portal/` landed — this direction extends it to the contract-named file).

---

## 1. Verification outcome

Every citation in the direction was re-checked against the repository at HEAD `40acef7` plus the working tree (B6-2 in-flight edits to the guard files, `docs/campaigns/implementation-gate.md` G7 row, and the untracked b6-2 specs/tests — none touches this direction's files). Executed: full guard four-file suite **+82 green** (`flutter test test/audit_contract_guard_test.dart test/audit_contract_guard_mutation_test.dart test/developer_audit_visibility_guard_test.dart test/oidc_login_audit_visibility_guard_test.dart -r expanded` → `+82: All tests passed!`, 2026-08-08); guard + mutation + `test/audit_log_tab_test.dart` → **104/104 green**.

**Three central claims of the direction are stale at HEAD, all verified:**

1. **The AC-1 present-branch seam test HAS landed** (`test/audit_log_tab_test.dart:448-505`, "present-branch: injected tenantId/traceId ride the wire (REQ-2)", committed `7a055be`). It pumps `AuditLogTab(api:, capabilities:, tenantId: 'acme', traceId: 'tr-1')`, asserts exactly one request with `queryParameters == {'limit':'100','tenant_id':'acme','trace_id':'tr-1'}` (`:470-473`), forged rows never render (`:483-484`), and the trim corner `'  acme  '/' tr-1 '` → `'acme'/'tr-1'` (`:489-505`). The default-branch pin the direction cites as "`:128-129`" now sits at `:170-172` (`{'limit':'100'}` + `containsKey('tenant_id')`/`containsKey('trace_id')` false) inside the AC-1/AC-3 group (`:151`+). `[CORRECTION: :128-129 → :170-172; "designed but not yet landed" → landed]`.
2. **The drill read leg HAS landed** (`tests/integration/audit_login_drill.py:219-275` Step 6, committed `3b64c58`): console-shaped `GET {API}/api/v1/audit/events?tenant_id=<t>&limit=100` (`:233-236`; helper `read_events` `:91-98`), settle, re-query, then a caller-attributed `audit.event.read` assertion with exact canonical type + actor pins (`:257-270`); unobserved → explicit `[proposed]` deviation, exit 0, never a false PASS (`:272-275`). Wired at `tests/integration/run_all.py:169` and `tests/integration/full_stack_verify.py:113`. `[CORRECTION: "designed but not yet landed" → landed]`.
3. **"grep of lib/ for claim parsing returns nothing" is false in the small**: `lib/api/portal_api.dart:61-94` contains client-side JWT payload claim parsing — `currentSessionId`/`_sidFromToken`/`_claimFromToken` (`:66-94`, RFC 9068 `sid`, OAuth `aud`) — the exact primitive class the direction says does not exist. What does not exist is **tenant_id claim extraction** (B4-1, still `[PROPOSED]`) and any trace_id injection mechanism. The existing helper is load-bearing for portal session revocation and trusted-device client scoping — it must not be repurposed (REQ-5).

| Direction citation | Verified repository reality | Status |
|---|---|---|
| `lib/screens/admin/dashboard_screen.dart:567` — the only `AuditLogTab(...)` construction, api + capabilities only | **Exact.** `:567` `page: AuditLogTab(api: _api, capabilities: capabilities),`; grep across `lib/` finds exactly this one production construction (all other hits are `test/`). Neither `tenantId` nor `traceId` is passed — the seam is unexercised in production. | ✅ |
| `lib/screens/admin/audit_log_tab.dart:40-46,69-73` — optional tenant/trace forwarded verbatim; "never derived, never hardcoded, never defaulted" | **Exact.** Doc comment `:29-34` ("…B4-1 claim parsing / proxy-side trace_id injection are `[PROPOSED]` and out of scope; when absent the wire stays exactly `{'limit':'100'}` (AC-1)"); fields `:34-35`, ctor params `:41-42`; `initState` `:67-70` → `AuditReadClient(widget.api, tenantId: widget.tenantId, traceId: widget.traceId)`. `[CORRECTION: :40-46 → :29-42]` | ✅ |
| `test/audit_log_tab_test.dart:128-129` — AC-1 default-wire pin | **Content exact, lines drifted** `[CORRECTION: → :170-172]` — `expect(uri.queryParameters, {'limit': '100'}); expect(uri.queryParameters.containsKey('tenant_id'), isFalse); expect(uri.queryParameters.containsKey('trace_id'), isFalse);` plus the search re-query pin at `:195` (identical `{'limit':'100'}`). | ✅ (drift) |
| `test/audit_log_tab_test.dart:143-145` — devtools-forged never render | **Content exact, lines drifted** `[CORRECTION]` — forged-never-renders asserts at `:187-188` (AC-1.5), `:346-347`/`:378-379`/`:409-410` (B6-1b group), `:483-484` (present-branch), and the T-12 raw-devtools on/off polarity tests at `:920`/`:943`. | ✅ (drift) |
| `lib/api/audit_read_client.dart:11-13,26-31,44-47` — trio literal owner; presence-iff-non-empty after trim | **Content exact, lines drifted** `[CORRECTION: trio :15-17; ctor :23-25; list() :35-56; facets() :65-84; event() :86-89]` — `eventsPath/facetsPath/eventDetailPath` sole literal owner (scan 7 pin); ctor takes optional `tenantId`/`traceId`; `list()`/`facets()` serialize exclusively via `AuditQuery` (`tenantId ?? _tenantId`); trim/omit semantics documented at `lib/api/audit_query.dart:14-16` and enforced by its serializer. | ✅ (drift) |
| `lib/api/portal_api.dart:39-43` — contract-named BFF client, zero audit methods | **Mostly exact, one material correction.** Doc comment `:31-32` lists the root-level `/me`-family paths; surface = `get/post/patch/put/delete` transport (`:110-209`), `login()` `:233-257` (GET `/me` probe `:245`), `logout()` `:268`, `fetchMe()` `:270-276`, notifications stream `:125`. `grep -ci audit lib/api/portal_api.dart` → **0** — zero audit surface, and `get()` has no `query:` parameter (an audit read cannot land there without inventing a surface). **Correction: `:61-94` contains JWT payload claim parsing (`_claimFromToken`, `sid`/`aud`) — see finding 3.** | ✅ (+correction) |
| `docs/campaigns/implementation-gate.md:56` — contract row 1 names `lib/api/portal_api.dart`（实际路径，QA F-11 实测；非 lib/services/） | **Exact — the open reconciliation gap.** Row 1 (console) still reads: "`lib/services/audit_log_service.dart`；`lib/api/portal_api.dart`（实际路径，QA F-11 实测；**非** `lib/services/`）；审计页 UI \| 读路径接入（F-06）：审计页调 sink 读 API（tenant_id + trace_id 经 BFF）…". The carrier decision record declares the parenthetical superseded, but **the gate row itself — the text QA F-11 path-audits — is unamended**. | ✅ (gap) |
| `docs/proposals/audit-contract-batch-snaplink-console.md:6,9` — `[PROPOSED]` markers (portal_api path scope; tenant_id/trace_id) | **Exact.** `:6` portal_api.dart scope 存疑 `[PROPOSED]`; `:9` tenant_id (token claim 解析, 依赖 B4-1) + trace_id (BFF 注入机制本仓无) `[PROPOSED]`. **Also `:10` `[RESOLVED]`（B6-1, 2026-08-08）carrier record exists** — it reconciles the read path (`SnaplinkAdminApi` via `AuditReadClient`, portal_api.dart 保持不动, gate `:56` parenthetical 以此为准) but (a) has not been propagated into the gate row, and (b) the reconciliation is not executable in the guard suite: `lib/api/portal_api.dart` is **outside scan 6's scope**. | ✅ (+:10 record) |
| `test/audit_contract_guard_scans.dart:170` — bff-literals scan | **Lines drifted, substance exact** `[CORRECTION: :170 is scan 1's token checker; scan 2 bff-literals at :193-216; scan 5 second-consumer :251-330; scan 7 trio-literal-owner :358+]`. Scan 6 (`portal-audit-boundary`) at `:458-475` covers `screens/portal/` **only** — `api/portal_api.dart` is not covered by any negative-boundary scan (its zero-audit state is unguarded today). | ✅ (drift + gap) |
| `docs/proposals/b6-1a-lib-screens-device-audit-read-client-gap-design.md` §1.6A — present-branch test designed | **Exact, and superseded by landing.** §1.6A at `:157-170` specs the pump/asserts; the landed test at `audit_log_tab_test.dart:448-505` implements it 1:1 (incl. the trim corner §1.6A calls for). | ✅ (landed) |
| B4-1 tenant/trace origin — "no such code exists in this repo" | **Partly false** — see finding 3. No `tenant_id` extraction and no trace_id injection exist; the JWT claim-decode primitive does (`portal_api.dart:61-94`). | ⚠️ correction |
| `audit_login_drill.py:79-82` — proxy drill read leg | **Exact, and landed.** `sink_rows()` `:78-89` (`GET {API}/api/v1/audit/events?event_types=auth.login.success&tenant_id=<t>`); Step 6 read leg `:219-275` (see finding 2). | ✅ (landed) |

**Bottom line:** all three acceptance checks are closed. The seam is exercised (present-branch test), the T-12 self-audit read leg is pinned (drill Step 6), the gate row names the proven AuditReadClient/SnaplinkAdminApi carrier, and the named portal_api.dart path is protected by the same fail-closed negative-boundary scan as lib/screens/portal/. REQ-3/REQ-4 remain verification records; REQ-1/REQ-2 are landed in this working tree.

## 2. Scope

**In scope (this direction's acceptance check 1 + landed-state verification of checks 2–3)**

- REQ-1 — Decision record + gate row amendment: `docs/campaigns/implementation-gate.md:56` row 1 corrected to name the landed carrier; the single decision record (`audit-contract-batch-snaplink-console.md:10`) extended with the codification note (one record, both files documented as reconciled).
- REQ-2 — Guard codification of the named path: scan 6 (`portal-audit-boundary`) extended to cover `api/portal_api.dart`; positive probe test; F2 wiring-pin probe; fail-closed mutation skin; guard-count re-pin (`Makefile` `GUARD_PIN_COUNT`, G7 row, Makefile comment — together).
- REQ-3 — Verification of the landed AC-1 present-branch seam test (no new code).
- REQ-4 — Verification of the landed drill read leg + forged-never-renders pins (no new code).
- REQ-5 — B4-1 boundary: tenant_id/trace_id stay caller-supplied-only; no claim parsing added; the existing `_claimFromToken` helper is recorded as out-of-bounds for tenant_id.

**Out of scope (explicitly not changed)**

- Any production change in `lib/` — zero diff, including `lib/screens/device/` (guard `test/device_audit_visibility_guard_test.dart` stays green), `lib/screens/admin/audit_log_tab.dart`, `lib/api/audit_read_client.dart`, `lib/api/portal_api.dart`, `lib/api/audit_query.dart`.
- B4-1 tenant_id claim extraction and proxy-side trace_id injection — `[PROPOSED]`, snaplink-side dependency; the seam already forwards caller-supplied values (REQ-3 verifies).
- The landed portal request-bound widget test and `b6-1-lib-screens-portal-negative-boundary-spec.md` REQ-1/REQ-2/REQ-3 — sibling deliverables, untouched.
- The drill harness wiring (`run_all.py`/`full_stack_verify.py`) and the B6-2 branch legs of `audit_login_drill.py`.
- Any new test in the count-pinned files beyond the two additions of REQ-2 (guard probe + mutation skin).

## 3. Requirements

### REQ-1 — One decision record: gate row names the landed carrier; the named path is marked not-a-path
The contract text QA F-11 path-audits must stop naming `lib/api/portal_api.dart` as the audit read path. Amend `docs/campaigns/implementation-gate.md:56` row 1 (改动 column) to:

> `lib/services/audit_log_service.dart`（调试 ring）；**读路径**：`lib/api/audit_read_client.dart` 经 `SnaplinkAdminApi`（B6-1a 落地；`lib/api/portal_api.dart` 非审计读路径——QA F-11 实测为门户自服务客户端，零 audit 表面，scan 6 负边界 pin）；审计页 UI

and amend the 改动 column's read-path clause from "审计页调 sink 读 API（tenant_id + trace_id 经 BFF）" to "审计页经 `AuditReadClient.list()` 调 sink 读 API；tenant_id/trace_id 经 BFF 为 B4-1 依赖（`[PROPOSED]`），seam 已就绪（caller-supplied-only），默认 off-wire 恰为 `{'limit':'100'}`". Then extend the single decision record — append one codification line to the existing `[RESOLVED]`（B6-1, 2026-08-08）record at `docs/proposals/audit-contract-batch-snaplink-console.md:10` — stating: gate row 1 amended (read path = `audit_read_client.dart` via `SnaplinkAdminApi`; `portal_api.dart` marked non-path), scan 6 scope extended to `api/portal_api.dart`, guard count re-pinned, and the AC-1 present-branch test (`audit_log_tab_test.dart:448-505`) + drill Step 6 (`audit_login_drill.py:219-275`) landed. No new record file — the `:10` record remains the single decision record.

**Testable:** `sed -n '56p' docs/campaigns/implementation-gate.md` → row 1 contains `lib/api/audit_read_client.dart` and `portal_api.dart` 非审计读路径, and no longer pairs `portal_api.dart` with （实际路径，QA F-11 实测）; `grep -n '\[RESOLVED\]' docs/proposals/audit-contract-batch-snaplink-console.md` → the B6-1 line at `:10` contains the codification note (gate row 1 amended + scan 6 scope + count re-pin); `git diff --stat docs/` limited to the two files.

### REQ-2 — Guard codification of the named path: scan 6 covers `api/portal_api.dart`, fail-closed, count re-pinned
Extend scan 6 (`portal-audit-boundary`, `test/audit_contract_guard_scans.dart:458-475`) so the **portal surface** = the UI module (`screens/portal/`) **and the contract-named client (`api/portal_api.dart`)** keep zero case-insensitive `audit` occurrences. Implementation shape: `scanPortalAuditBoundary` unions two `_scanNegativeBoundary` calls (prefixes `screens/portal/` and `api/portal_api.dart`, same scan id `portal-audit-boundary`, same `boundaryReason` — the screens/portal detail string stays byte-identical so the existing V1 pin at `test/audit_contract_guard_test.dart:530-545` holds unedited); update the scan-6 doc comment to cite the decision record (`audit-contract-batch-snaplink-console.md:10`) and the amended gate row 1. Codification pins, all in the count-pinned files:

1. **Positive probe test** (new, in the scan-6 group `test/audit_contract_guard_test.dart:320-437`): a probe labeled `api/portal_api.dart` containing `audit` trips `scanPortalAuditBoundary` with scan id `portal-audit-boundary` (mirror of the V1 pin).
2. **F2 wiring-pin extension** (body edit of the existing test at `:330-437`): the dirty probe tree gains `api/portal_api.dart` containing `audit`; the clean control neutralizes it to `'activity'` — the new prefix is proven wired through `scanLibDirectory` and non-vacuous.
3. **Fail-closed mutation skin** (new test in `test/audit_contract_guard_mutation_test.dart` scan-6 group `:377-437`): an in-memory override of `lib/api/portal_api.dart` (e.g., an `AuditReadClient` import + usage, or an `audit` identifier) trips `portal-audit-boundary` via `_scanWith` (the default scans set already includes the id).
4. The existing green-against-tree test (`scan 6 — zero "audit" occurrences across the live portal module`, `:322-327`) covers the new prefix automatically (whole-lib scan) — no duplicate green test.
5. **Guard-count re-pin**: the suite gains **+1** new test (the PortalApi mutation skin; the F2 body edit adds none). Re-measure `flutter test test/audit_contract_guard_test.dart test/audit_contract_guard_mutation_test.dart test/developer_audit_visibility_guard_test.dart test/oidc_login_audit_visibility_guard_test.dart -r expanded`, then bump `Makefile` `GUARD_PIN_COUNT` (82 → 83) and the `implementation-gate.md` G7 row breakdown **and** the Makefile comment **in the same change set**. Landing order: this re-pin measures the **combined final tree** (the in-flight B6-2 edits to these two files land first or together; never re-pin against a half-state).

**Testable:** `make guard-count-pin` → `guard count pin: OK (+83: All tests passed!)`; `flutter test test/audit_contract_guard_test.dart test/audit_contract_guard_mutation_test.dart` green; a manual probe — `scanPortalAuditBoundary("final _audit = 0;", 'api/portal_api.dart')` — non-empty (the new prefix trips); `grep -ci audit lib/api/portal_api.dart` → 0 (unchanged).

### REQ-3 — AC-1 present-branch seam test: landed state, no re-landing (acceptance check 2)
The direction's acceptance check 2 is satisfied by the landed test — verify, do not re-land. Pins: `test/audit_log_tab_test.dart:448` (title "present-branch: injected tenantId/traceId ride the wire (REQ-2)"), `:457-473` (pump with `tenantId: 'acme', traceId: 'tr-1'` → exactly one request to `GET /api/v1/audit/events`, `queryParameters == {'limit':'100','tenant_id':'acme','trace_id':'tr-1'}`), `:477-484` (rows from the mock server body; forged text never renders; no `/facets`, no `/events/{id}`), `:489-505` (trim corner: `'  acme  '/' tr-1 '` serialize to `'acme'/'tr-1'`). Default branch untouched: `:170-172` keeps the `{'limit':'100'}`-only pin with `tenant_id`/`trace_id` absent, and `:195` the identical search re-query pin. The seam contract (gap-spec REQ-2 "present-or-absent, never silent"; `audit_log_tab.dart:29-34` "never derived, never hardcoded, never defaulted") is unchanged.

**Testable:** `flutter test test/audit_log_tab_test.dart` → green (verified 104/104 with the guard files, 2026-08-08); `git diff --exit-code -- test/audit_log_tab_test.dart` after this direction lands → clean (no edit to this file).

### REQ-4 — Drill read leg + forged-never-renders: landed state, no re-landing (acceptance check 3)
The direction's acceptance check 3 is satisfied by the landed drill Step 6 — verify, do not re-land. Pins: `tests/integration/audit_login_drill.py:219-275` (Step 6: console-shaped `GET {API}/api/v1/audit/events?tenant_id=<t>&limit=100` `:233-236`, settle via `settle_seconds()`, re-query via `read_events` `:91-98`, caller-attributed `audit.event.read` assertion with exact canonical-type + actor pins `:257-270` — a concurrent same-tenant read cannot decoy a PASS; unobserved → `[proposed]` deviation, exit 0, **never a false PASS** `:272-275`); wired at `run_all.py:169` / `full_stack_verify.py:113` (zero bytes of either harness). Forged-never-renders (the "devtools-forged `sso_audit_log` entries never render" half of T-12): T-12 raw-devtools on/off polarity tests at `audit_log_tab_test.dart:920`/`:943` (raw `LocalStorage.setItem('sso_audit_log', …)` seeding, forged text `findsNothing` + byte-identical off-polarity), AC-1.5 `:187-188`, B6-1b forged asserts `:346-347`/`:378-379`/`:409-410`, present-branch `:483-484`.

**Testable:** `python3 tests/integration/audit_login_drill.py` → without a configured stack: `SKIP:` cleanly (never PASS/FAIL); with a stack: Step 6 prints either a PASS (caller-attributed `audit.event.read` row) or an explicit `[proposed]` deviation — no false PASS by construction (FM-15 path contract in the gap-design §1.6B). Widget side: `flutter test test/audit_log_tab_test.dart` green.

### REQ-5 — B4-1 boundary: tenant_id/trace_id stay caller-supplied-only; no claim parsing added
The origin of tenant/trace remains B4-1-dependent and `[PROPOSED]` — this direction adds **no** tenant_id extraction and **no** trace_id injection. Correction recorded (finding 3): the JWT payload-decode helper `_claimFromToken` (`lib/api/portal_api.dart:61-94`, `sid`/`aud`) already exists and is load-bearing for portal session revocation / trusted-device client scoping; it must not be repurposed for tenant_id (that is B4-1 work, out of scope), and the `bff-literals` scan keeps any BFF token banned from `lib/` (a future proxy-side trace_id injection would need a `[PROPOSED]` review against the b6-1a proxy-log leak boundary, `b6-1a-lib-api-auditlogtab-server-read-spec.md:51`). The seam contract is: constructor-injected values ride the wire verbatim (REQ-3), absent values keep the wire exactly `{'limit':'100'}`.

**Testable:** `git diff --stat lib/` → empty after this direction lands (zero production diff); `grep -rn "tenant_id" lib/` unchanged from HEAD (serialization only in `audit_query.dart`/`audit_read_client.dart`); `make guard-count-pin` green.

## 4. Acceptance checks (the three supplied checks, preserved 1:1 and made testable)

**AC-1 — Supplied check (1): contract-gate scan extended/amended; both paths documented as reconciled; one decision record; scans stay green.**
1. Gate row: `sed -n '56p' docs/campaigns/implementation-gate.md` → row 1 names `lib/api/audit_read_client.dart` 经 `SnaplinkAdminApi` as the read path and marks `lib/api/portal_api.dart` 非审计读路径（scan 6 负边界 pin）; the （实际路径，QA F-11 实测） claim is gone.
2. One decision record: `grep -n '\[RESOLVED\]' docs/proposals/audit-contract-batch-snaplink-console.md` → the B6-1 line at `:10` contains the codification note covering **both** files (named path `portal_api.dart` = non-path, scan-pinned; landed owner `audit_read_client.dart` = carrier) and the count re-pin; no second record file is created.
3. Guard codification (REQ-2): `scanPortalAuditBoundary` trips on an `api/portal_api.dart`-labeled probe; F2 wiring pin includes the new prefix in both dirty and clean probe trees; mutation skin trips `portal-audit-boundary` on an in-memory `lib/api/portal_api.dart` override.
4. Scans stay green: `make guard-count-pin` → `OK (+83: All tests passed!)` after the together-re-pin; `grep -ci audit lib/api/portal_api.dart` → 0.

**AC-2 — Supplied check (2): present-branch widget test (designed in `b6-1a-lib-screens-device-audit-read-client-gap-design.md` §1.6A) pumps AuditLogTab with tenantId/traceId through the seam and asserts the wire carries them verbatim; the default branch keeps the `{'limit':'100'}`-only pin. — LANDED (verify, don't re-land).**
- `test/audit_log_tab_test.dart:448-505` green: `tenantId: 'acme', traceId: 'tr-1'` → exactly one request, `queryParameters == {'limit':'100','tenant_id':'acme','trace_id':'tr-1'}`; trim corner `'  acme  '/' tr-1 '` → trimmed values; forged text never renders.
- Default branch intact: `:170-172` — `queryParameters == {'limit':'100'}`, `containsKey('tenant_id')` false, `containsKey('trace_id')` false; search re-query pin `:195` identical.
- `flutter test test/audit_log_tab_test.dart` green; `git diff --exit-code -- test/audit_log_tab_test.dart` clean after landing.

**AC-3 — Supplied check (3): drill read leg queries `GET /api/v1/audit/events` with tenant_id and asserts the self-audit row appears; devtools-forged `sso_audit_log` entries never render. — LANDED (verify, don't re-land).**
- `tests/integration/audit_login_drill.py:219-275` (Step 6): console-shaped query `?tenant_id=<t>&limit=100`, settle, re-query, caller-attributed `audit.event.read` assertion (`:257-270`); unobserved → explicit `[proposed]`, exit 0, never a false PASS (`:272-275`); wired `run_all.py:169` / `full_stack_verify.py:113`.
- Forged never renders: T-12 on/off polarity tests `audit_log_tab_test.dart:920`/`:943` (raw `LocalStorage.setItem` seeding, `findsNothing` + byte-identical off polarity), AC-1.5 `:187-188`, present-branch `:483-484` — all green.
- `python3 tests/integration/audit_login_drill.py` → clean SKIP without a stack; `flutter test test/audit_log_tab_test.dart` green.

## 5. Risk and rollback

- **Count-pin coupling (highest):** the new PortalApi mutation skin (+1) and the extended F2 wiring pin make `make guard-count-pin` red until `GUARD_PIN_COUNT`, the G7 row, and the Makefile comment are re-pinned **in the same change set** (F8 silent-regression class — the pin exists precisely to force this). Rollback: revert the four-file guard edits + re-pin together; the count pin is the tripwire.
- **Scan-6 detail-string stability:** the extension keeps the `screens/portal/` branch's `moduleLabel`/`boundaryReason` byte-identical, so the V1 pin (`audit_contract_guard_test.dart:530-545`) holds unedited; only the new `api/portal_api.dart` branch carries its own label. A mis-merge that changes the shared `boundaryReason` reds the V1 pin immediately.
- **Landing-order interlock:** the working tree carries in-flight B6-2 edits to the guard files and the G7 row; this direction's re-pin must measure the combined final tree — never a half-state (REQ-2 item 5).
- **Scope discipline:** zero `lib/` diff (REQ-5 testable form) — the reconciliation is docs + tests only, so any misstep is revertible without production risk; `lib/screens/device/` stays untouched (device guard green).
- **No false-PASS risk added:** drill Step 6 already fails closed (`[proposed]` on unobserved); this direction adds no drill code.
