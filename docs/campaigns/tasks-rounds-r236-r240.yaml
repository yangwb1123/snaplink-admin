# R236-R240：Portal Security Activity 的静态文案 i18n 与 API 值边界
# 目标：修复已登记静态事件标题/标记在 security activity timeline 中未经过 locale 的问题。
# API wire、/me 路径、认证、Session/MFA/Consent、设备码、Checkout、secret、导出、audit trio
# 与 SSE 语义保持不变；未知服务端值必须原样显示，不得强制翻译。
# 运行：python3 ~/ai-batch-runner/pi-batch.py docs/campaigns/tasks-rounds-r236-r240.yaml --mode serial --no-git-commit --kill-orphans --timeout 900 --dry-run
#      python3 ~/ai-batch-runner/pi-batch.py docs/campaigns/tasks-rounds-r236-r240.yaml --mode serial --no-git-commit --kill-orphans --timeout 900 --agent-bin /usr/local/lib/node_modules/@earendil-works/pi-coding-agent/dist/bundle/cli.js

tasks:
- prompt: |
    R236：Portal SecurityActivity timeline 的 i18n 边界审计与最小修复。读取
    `lib/screens/portal/security_activity_tab.dart`、`lib/screens/portal/portal_widgets.dart`、
    `lib/widgets/timeline_list.dart`、`lib/i18n/app_strings_source_portal.dart`、
    `test/portal_security_widgets_test.dart`、`test/portal_screens_test.dart`。

    追踪 activity/history 的静态 UI keys、已登记 event title/flag keys 与 API-provided
    detail/location/ip/device/provider/type 值。实测 en/zh：当前 `_toTimelineItem`、
    `_toHistoryItem`、`_eventTitle` 产出的已登记静态标题/标记是否绕过 locale；未知 type、
    server detail 和标识符是否应保持原样。若确认静态 key 漏翻，只把 BuildContext 或等价
    locale resolver 传到本地 projection，在既有 `app_strings_source_portal.dart` key 范围内
    使用 `context.tr`；不要改 `PortalSecurityPaths`、请求顺序、响应解析、timeline 数据
    结构、API 值或 portal/developer/oidc_login 的英文 `audit` 文案。补最小 en/zh 与 unknown
    value 回归测试，输出 `docs/ui/pages-per-page/r236-security-activity-i18n.md`。
  output: docs/ui/pages-per-page/r236-security-activity-i18n.md
  validate: "cd {cwd} && dart format --output=none --set-exit-if-changed lib/screens/portal/security_activity_tab.dart test/portal_security_widgets_test.dart && flutter analyze --no-fatal-infos --no-fatal-warnings && flutter test test/portal_security_widgets_test.dart test/portal_screens_test.dart test/i18n_coverage_test.dart test/i18n_catalog_uniqueness_test.dart && git diff --check"

- prompt: |
    R237：Security Activity 的响应式与语义回归。读取 R236 报告、当前 diff、
    `lib/screens/portal/security_activity_tab.dart`、`lib/widgets/timeline_list.dart`、
    `test/portal_security_widgets_test.dart`、`test/render_matrix_smoke_test.dart`。
    在 240/320/400/640/768/1200px、zh locale、2x text scale 下验证标题/标记换行或收敛、
    长 API detail/location/ip/device/provider 不横溢出，timeline list/listItem 语义、
    live error region、refresh callback 与 reduced-motion 不变。Security Activity 仍只请求
    `/me/security/activity` 与 `/me/login-history`，关系/线性数据渲染规则不变；只修复本批
    直接引入的明确回归，不机械拆分、不增加依赖、不改 endpoint/route/auth/security。
    输出 `docs/ui/pages-per-page/r237-security-activity-responsive.md`。
  output: docs/ui/pages-per-page/r237-security-activity-responsive.md
  validate: "cd {cwd} && flutter analyze --no-fatal-infos --no-fatal-warnings && flutter test test/portal_security_widgets_test.dart test/portal_screens_test.dart test/render_matrix_smoke_test.dart test/portal_security_web_test.dart && git diff --check"

- prompt: |
    R238：i18n/API/安全合同复核。读取 R236-R237 报告、完整当前 diff、
    `lib/i18n/app_strings_source_portal.dart`、`test/i18n_coverage_test.dart`、
    `test/i18n_catalog_uniqueness_test.dart`、`test/portal_security_contract_test.dart`、
    `test/portal_api_test.dart`、`test/portal_notifications_test.dart`、
    `test/sensitive_data_test.dart`、`test/admin_paths_test.dart`、`test/audit_read_client_test.dart`。

    证明只增加已登记静态 security activity 翻译投影与回归：API detail/location/ip 等值、
    unknown event type、timestamps、device/provider 不作为翻译 key；不新增 identity source
    pattern 或 catalog key；Portal activity 不调用固定 audit browser trio，Portal/Admin
    ownership、segment-aware prefix matching、single encode、client_id、explicit
    AUDIT_GOVERNANCE_UPSTREAM、SSE lifecycle、secret/redaction/one-time acknowledge 均不变。
    发现问题只作最小修复，否则只报告
    `docs/ui/pages-per-page/r238-security-activity-contract-review.md`。
  output: docs/ui/pages-per-page/r238-security-activity-contract-review.md
  validate: "cd {cwd} && flutter analyze --no-fatal-infos --no-fatal-warnings && flutter test test/i18n_coverage_test.dart test/i18n_catalog_uniqueness_test.dart test/portal_security_contract_test.dart test/portal_api_test.dart test/portal_notifications_test.dart test/sensitive_data_test.dart test/admin_paths_test.dart test/audit_read_client_test.dart && python3 -m unittest discover -s tests/unit -p 'test_*.py' && git diff --check"

- prompt: |
    R239：结构与台账中间复核。读取 R236-R238 报告、`docs/TECH_DEBT.md`、`engineering.yaml`、
    `lib/screens`、Portal widgets 和 `docs/ui` 清单。只同步已实测数字：lib/screens 仍 248
    且全部 <=400 行，Developer census 13，source/ZH 2313/2313，identity pattern 0，
    appAdditionalStrings 105/105，`docs/ui` 顶层 <=12；pages-per-page 此时应为 314（包含
    R236-R239 报告）。保持 routes 215/scoped 214、AdminPaths 122、/run non-root、显式
    `AUDIT_GOVERNANCE_UPSTREAM` fail-closed、BFF `[PROPOSED]` 与 heuristic 非阻塞说明。
    Security Activity 不做为减少行数的机械拆分；若更新 TECH_DEBT，仅更新标题/当前轮次与
    该中间报告数。输出 `docs/ui/pages-per-page/r239-security-activity-ledger.md`。
  output: docs/ui/pages-per-page/r239-security-activity-ledger.md
  validate: "cd {cwd} && python3 checks/filesize.py && python3 checks/directory_fanout.py && python3 -m unittest discover -s tests/unit -p 'test_*.py' && flutter test test/oidc_login_handle_success_census_test.dart test/app_router_client_id_wiring_test.dart test/i18n_catalog_uniqueness_test.dart && git diff --check"

- prompt: |
    R240：最终门禁。读取 R236-R239 报告和完整当前 diff，确认只保留有证据的 Security Activity
    静态文案 locale projection、API 值原样边界、回归测试、campaign YAML 与台账；拒绝任何
    endpoint/route/auth/security 重构。更新 `docs/TECH_DEBT.md` 至 R240，并同步
    pages-per-page 当前报告数为 315（R236-R240）。

    确认 `SecurityActivityTab` 仍仅访问 `/me/security/activity` 与 `/me/login-history`，不
    触碰 audit trio；timeline/list/card 规则、Dashboard `leadingWidth: 80`、Developer
    13-file census、固定 `client_id`、Portal/Admin owner、SSE、audit browser paths/upstream、
    secret redaction/one-time acknowledge，以及预先存在的 `web/flutter_bootstrap.js` 未跟踪
    文件不变。

    串行运行并记录：受影响 Dart format、`flutter analyze --no-fatal-infos --no-fatal-warnings`、
    全量 `flutter test`、Chrome smoke `flutter test --platform chrome test/widget_test.dart test/portal_security_web_test.dart test/federated_login_web_test.dart`、
    `make build-prod`、`make release-artifact-check`、Python unit、`python3 cli.py harness`、
    `python3 checks/filesize.py`、`python3 checks/directory_fanout.py`、`git diff --check`。
    只有全部门禁通过才在输出最后一行写严格的
    `VERDICT: PASS - ...`；失败则写 `VERDICT: FAIL - ...`，不要加 Markdown backticks。
    输出 `docs/ui/pages-per-page/r240-final-audit-34.md`。
  output: docs/ui/pages-per-page/r240-final-audit-34.md
  validate: "cd {cwd} && python3 checks/filesize.py && python3 checks/directory_fanout.py && flutter analyze --no-fatal-infos --no-fatal-warnings && flutter test && flutter test --platform chrome test/widget_test.dart test/portal_security_web_test.dart test/federated_login_web_test.dart && make build-prod && make release-artifact-check && python3 -m unittest discover -s tests/unit -p 'test_*.py' && python3 cli.py harness && git diff --check && tail -1 {output} | grep -Eq '^VERDICT: (PASS|FAIL) - .+$'"
